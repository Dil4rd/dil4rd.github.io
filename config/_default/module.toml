# Congo is consumed as a Hugo Module. The exact version is pinned in go.mod.
# Upgrade with: hugo mod get -u github.com/jpanther/congo/v2

[hugoVersion]
  extended = true
  min = "0.158.0"

[[imports]]
  path = "github.com/jpanther/congo/v2"
